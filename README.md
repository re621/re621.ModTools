A collection of utilities for using and moderating the booru site e621.

## Downstream projects
Take a careful look at `package.json`, `bin/userscript-header.js`, & `src/js/models/data/Script.ts`; these 3 points are the main source of project-specific configuration. If done properly, modifying these 3 files for your project's needs should minimize merge conflicts with this project.